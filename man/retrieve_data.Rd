% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve_data.R
\name{retrieve_data}
\alias{retrieve_data}
\title{Retrieve observations from a .Stat Suite SDMX Dataflow}
\usage{
retrieve_data(
  environment,
  data_space,
  agency_id,
  dataflow_id,
  dataflow_version,
  ...,
  dimensionAtObservation = "AllDimensions",
  format = "csvfilewithlabels"
)
}
\arguments{
\item{environment}{Character(1). `"qa"` or `"prod"` (case‑insensitive),
passed to [build_root_url()].}

\item{data_space}{Character(1). Data‑space prefix
(e.g. `"design"`, `"collection"`, `"processing"`,
`"staging"`, `"disseminate"`, `"archive"`).
**Do not** include `https://`.}

\item{agency_id}{Character(1). SDMX agency ID owning the dataflow
(e.g. `"OECD"`).}

\item{dataflow_id}{Character(1). Dataflow code without agency or version
(e.g. `"SNA_TABLE1"`).}

\item{dataflow_version}{Character(1). Version of the dataflow (e.g. `"1.0"`).}

\item{...}{Additional named SDMX query parameters, such as
`startPeriod`, `endPeriod`, or specific dimension filters.}

\item{dimensionAtObservation}{Character(1). SDMX `dimensionAtObservation` parameter;
defaults to `"AllDimensions"`.}

\item{format}{Character(1). SDMX `format` parameter;
defaults to `"csvfilewithlabels"`.}
}
\value{
A tibble (`tbl_df`) of the observations returned by the API.
}
\description{
Retrieve observations from a .Stat Suite SDMX Dataflow
}
\examples{
\dontrun{
df <- retrieve_data(
  environment      = "qa",
  data_space       = "design",
  agency_id        = "OECD",
  dataflow_id      = "SNA_TABLE1",
  dataflow_version = "1.0",
  startPeriod      = "2020",
  endPeriod        = "2021"
)
}
}
\seealso{
[retrieve_structure()], [build_root_url()]
}
